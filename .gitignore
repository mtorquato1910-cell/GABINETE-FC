# Pastas de input local — imagens brutas que NÃO vão pro Git
# (têm nomes longos que estouram limite do Windows, e são processadas pelo script)
/Camisas destaques/
/imagens-raw/

.vercel

# Claude Code settings local — pode conter allowlist com tokens/secrets
.claude/settings.local.json
